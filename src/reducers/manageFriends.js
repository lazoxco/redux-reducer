export function manageFriends(state = {friends: []}, action){

  switch(action.type) {
    case 'ADD_FRIEND':
      // add a friend
    case 'REMOVE_FRIEND':
      // remove a friend
    default:
      // set default
  }
}
